{
  "name": "universal-sub-agent",
  "version": "1.3.3",
  "description": "Chrome extension: select any text on any webpage to get instant AI explanations. Multi-window, multi-provider, KaTeX math, Shadow DOM isolation.",
  "private": true,
  "license": "MIT",
  "scripts": {
    "check": "node --check background.js && node --check content.js && node --check options.js && node --check popup.js && node --check shared/config.js",
    "test": "node tests/test_formula_partial_and_nested.js && node tests/test_formula_selection.js && node tests/test_full_pipeline_render.js && node tests/test_nested_context.js && node tests/test_selection_location.js && node tests/test_system_prompt.js && node tests/test_unicode_symbols.js",
    "build": "node scripts/build.js"
  },
  "devDependencies": {
    "archiver": "^7.0.1",
    "jsdom": "^29.0.0"
  }
}
